// const {videos} = require('youtube.js');

// const { UniversalCache, Innertube } = require('youtubei.js');
// const { SlashCommandBuilder } = require('discord.js');

// const yout = Innertube.create({cache: new UniversalCache(false), generate_session_locally:true});

// const channel = await yout.getChannel(chan);
// const videos = await channel.getVideos();

// module.exports = {
// data: new SlashCommandBuilder()
//		.setName('yt')
//		.setDescription('see info about any yt channel')
 //       .addStringOption(chan =>
 //           chan.setName('channel')
 //               .setDescription('needed channel')),
//			async execute(interaction) {
//		await interaction.reply(videos);

//	},
// };
